<form method="post" action="{{ path('app_mean_crud_delete', {'id': exams_mean.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exams_mean.id) }}">
    <button class="btn">Delete</button>
</form>
