{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ student.Name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ student.Email }}</td>
            </tr>
            <tr>
                <th>Mention</th>
                <td>{{ student.Mention }}</td>
            </tr>
            <tr>
                <th>Degree</th>
                <td>{{ student.Degree }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_student_crud_index') }}">back to list</a>

    <a href="{{ path('app_student_crud_edit', {'id': student.id}) }}">edit</a>

    {{ include('student_crud/_delete_form.html.twig') }}
{% endblock %}
