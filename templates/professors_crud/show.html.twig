{% extends 'base.html.twig' %}

{% block title %}Professors{% endblock %}

{% block body %}
    <h1>Professors</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ professor.Name }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ professor.Contact }}</td>
            </tr>
            <tr>
                <th>Discipline</th>
                <td>{{ professor.discipline }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_professors_crud_index') }}">back to list</a>

    <a href="{{ path('app_professors_crud_edit', {'id': professor.id}) }}">edit</a>

    {{ include('professors_crud/_delete_form.html.twig') }}
{% endblock %}
